{
	"name": "table.isempty",
	"invocations": [
		{
			"rettype": "boolean",
			"args": [
				{
					"type": "table",
					"name": "self"
				}
			],
			"description": "Checks wheter given table is empty, that is, has no elements. This may be needed for tables with non-numeric indexes, where the length operator (#) might not work properly. NOTE: May return incorrect values if the given table contains nil values."
		}
	]
}
