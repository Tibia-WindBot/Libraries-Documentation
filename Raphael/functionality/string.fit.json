{
	"name": "string.fit",
	"invocations": [
		{
			"rettype": "string",
			"args": [
				{
					"type": "string",
					"name": "self"
				},
				{
					"type": "number",
					"name": "size"
				},
				{
					"type": "optional string",
					"name": "trailing"
				},
				{
					"type": "optional boolean",
					"name": "trueSize"
				}
			],
			"description": "Makes sure the given string fits in the given size. If set, adds <i>trailing</i> to end of it. If <i>trueSize</i> is set to false, the comparison is made using the string length; if it's set to true, the comparison is made using the actual string width, in pixels, gathered by using <i>measurestring</i>."
		}
	]
}
