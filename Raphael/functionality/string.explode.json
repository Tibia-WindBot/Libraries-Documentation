{
	"name": "string.explode",
	"invocations": [
		{
			"rettype": "array",
			"args": [
				{
					"type": "string",
					"name": "self"
				},
				{
					"type": "string",
					"name": "delimiter"
				}
			],
			"description": "Splits the string by the specified delimiter. Returns an array of strings, each of which is a substring of self formed by splitting it on boundaries formed by the string delimiter."
		}
	]
}
