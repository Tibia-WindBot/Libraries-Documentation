{
	"name": "screentiles",
	"invocations": [
		{
			"rettype": "function",
			"args": [
				{
					"type": "optional function",
					"name": "order"
				},
				{
					"type": "optional number",
					"name": "range",
				},
				{
					"type": "optional function",
					"name": "mod",
				}
			],
			"description": "Returns an iteration loop between screen tiles coordinates. Optionally you can set a <i>order</i> sorting function to sort tiles. Optionally you can set the <i>range</i> distance get coordinates. Optionally you can add a <i>mod</i> function to modify use the coordinates as parameter and return the value generated by the function."
		},
		{
			"rettype": "function",
			"args": [
				{
					"type": "optional function",
					"name": "order"
				},
				{
					"type": "optional table",
					"name": "positions",
				},
				{
					"type": "optional function",
					"name": "mod",
				}
			],
			"description": "Returns an iteration loop between screen tiles coordinates. Optionally you can set a <i>order</i> sorting function to sort tiles. Optionally you can set the <i>range</i> distance get coordinates. Optionally you can add a <i>mod</i> function to modify use the coordinates as parameter and return the value generated by the function."
		}
	]
}
