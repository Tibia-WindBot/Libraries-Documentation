{
	"name": "table.tostring",
	"invocations": [
		{
			"rettype": "string",
			"args": [
				{
					"type": "table",
					"name": "tbl"
				},
				{
					"type": "optional string",
					"name": "separator"
				}
				],
			"description": "Converts a table to a string variation exactly how the table is."
		},
		{
			"rettype": "string",
			"args": [
				{
					"type": "table",
					"name": "tbl"
				},
				{
					"type": "optional string",
					"name": "name"
				},
				{
					"type": "optional string",
					"name": "separator"
				}
				],
			"description": "Converts a table to a string variation exactly how the table is. If the <i>name<i> is given it'll return a string as \"name = {...}"."
		}
	]
}
